b = 84
c = b
if (a /= 0) {
    b *= 100
    b += 100000
    c = b
    c += 17000
}
while (true) { f = 1
    d = 2
    do { e = 2
        do { g = d
            g *= e
            g -= b
            if (g == 0)
                f = 0
            e++
            g = e
            g -= b
        } while (g /= 0)
        d++
        g = d
        g -= b
    } while (g /= 0)
    if (f == 0)
        h++
    g = b
    g -= c
    if (g == 0)
        return
    b += 17
}